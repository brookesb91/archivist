extends _layout
include ./components/thread

block append styles
  link(rel="stylesheet", href="/styles/thread.css")

block append content
  .thread-page
    .thread-header.p-lg.flex.flex-row.items-center.font-bold
      .title
        span.thread-board="/" + thread.board + "/"
        span.thread-id=thread.thread
    .thread-meta.p-lg.flex.flex-col.items-center.items-start.font-sm
      span="Archived on "
        time(datetime=thread.createdAt)=thread.createdAt
      span="Updated on " + thread.updatedAt
    +thread(thread)

block append scripts
  script(src="/scripts/thread.js", type="text/javascript")
  script(src="/scripts/popover.js", type="text/javascript")